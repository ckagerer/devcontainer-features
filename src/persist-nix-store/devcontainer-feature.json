{
    "name": "persist-nix-store",
    "id": "persist-nix-store",
    "version": "1.0.1",
    "description": "Mounts a named volume at /nix/store to persist the Nix store across devcontainer rebuilds. Use together with the chezmoi feature (defer_scripts=true) to avoid re-downloading packages on every rebuild.",
    "documentationURL": "https://github.com/ckagerer/devcontainer-features/tree/main/src/persist-nix-store",
    "options": {},
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ],
    "mounts": [
        {
            "source": "devcontainer-nix-store",
            "target": "/nix/store",
            "type": "volume"
        }
    ]
}
