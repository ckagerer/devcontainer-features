# Persist bash/zsh history (persist-shell-history)

Retain the history of bash and zsh even during rebuilds.

## Example Usage

```json
"features": {
    "ghcr.io/ckagerer/devcontainer-features/persist-shell-history:1": {}
}
```
